@model IEnumerable<Core_Health_and_Fitness.Models.PersonalTrainer>

@{
    ViewData["Title"] = "PersonalTrainers";
}

<h1>Personal Trainers List</h1>

<p>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.FirstName)
    </td>
    <td>

        @Html.ActionLink("Details", "Details", new { id = item.PersonalTrainerId })

    </td>
</tr>}
    </tbody>
</table>

